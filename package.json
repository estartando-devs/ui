{
  "name": "@estartando/monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev:example": "bun run --filter @estartando/example dev",
    "lint": "bun run --filter '*' lint"
  }
}
